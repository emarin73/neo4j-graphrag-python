# ====================================================
# Environment Variables Template for Neo4j GraphRAG
# ====================================================
#
# Copy this content into a file named ".env" in your project root directory.
# Only copy the KEY=VALUE lines below (ignore these comment lines).
#
# Note: The .env file is automatically ignored by git for security.
#
# ====================================================

# Neo4j Connection Configuration
NEO4J_URI=neo4j://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=your_password_here
NEO4J_DATABASE=neo4j

# For Neo4j AuraDB (cloud), uncomment and use:
# NEO4J_URI=neo4j+s://xxxxx.databases.neo4j.io

# OpenAI API Key (required for LLM and embeddings)
OPENAI_API_KEY=your_openai_api_key_here

# Optional: Specify which OpenAI model to use
# Recommended models:
#   - gpt-4o-mini: Faster, cheaper, 200k TPM rate limit (good for large PDFs)
#   - gpt-4o: More capable, 30k TPM rate limit (better quality, slower)
# OPENAI_MODEL=gpt-4o-mini

# Optional: Rate Limit Configuration
# RATE_LIMIT_MAX_ATTEMPTS=5
# RATE_LIMIT_MIN_WAIT=2.0
# RATE_LIMIT_MAX_WAIT=120.0
# RATE_LIMIT_MULTIPLIER=2.0

# ====================================================
# REQUIRED VARIABLES:
# - NEO4J_PASSWORD: Your Neo4j database password
# - OPENAI_API_KEY: Your OpenAI API key
#
# OPTIONAL VARIABLES:
# - NEO4J_URI: Default is "neo4j://localhost:7687"
# - NEO4J_USER: Default is "neo4j"
# - NEO4J_DATABASE: Default is "neo4j"
# - OPENAI_MODEL: Default is "gpt-4o"
# ====================================================
